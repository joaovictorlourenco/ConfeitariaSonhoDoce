---
import heroImg from "@/assets/hero-img.jpg";
import { Button } from "@/components/ui/button";
import { Image } from "astro:assets";
import { ArrowRight } from "lucide-react";
---

<section id="inicio" class="relative overflow-hidden bg-accent/30">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-20 lg:py-32">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <div class="space-y-8">
        <div class="space-y-4">
          <h1
            class="text-5xl sm:text-6xl lg:text-7xl font-bold text-balance leading-tight"
          >
            Doçura que <span class="text-primary">encanta</span> e <span
              class="text-primary">aquece</span
            >{" "}
            o coração
          </h1>
          <p
            class="text-lg sm:text-xl text-muted-foreground leading-relaxed max-w-xl"
          >
            Doces artesanais feitos com amor e ingredientes selecionados. Cada
            mordida é uma experiência única de sabor e felicidade.
          </p>
        </div>
        <div class="flex flex-col sm:flex-row gap-4">
          <Button
            size="lg"
            className="rounded-full text-base group flex flex-row items-center"
          >
            <a href="#contato"> Peça Agora </a>
            <ArrowRight
              className="ml-2 h-5 w-5 group-hover:translate-x-1 transition-transform"
            />
          </Button>
          <Button
            size="lg"
            variant="outline"
            className="rounded-full text-base bg-transparent"
          >
            <a href="#produtos">Ver Produtos</a>
          </Button>
        </div>
      </div>

      <div class="relative">
        <div class="aspect-square rounded-3xl overflow-hidden bg-muted">
          <Image
            src={heroImg}
            alt="Doces artesanais da Sonho Doce"
            class="w-full h-full object-cover"
          />
        </div>
        <div
          class="absolute -bottom-6 -left-6 bg-card p-6 rounded-2xl shadow-lg border border-border"
        >
          <p class="text-sm text-muted-foreground font-mono">Feito com</p>
          <p class="text-2xl font-bold text-primary">❤️ Amor</p>
        </div>
      </div>
    </div>
  </div>
</section>
